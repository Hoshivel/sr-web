# sr-web 前端建置期環境變數（複製為 .env 後修改；`PUBLIC_` 前綴才會進 client bundle）
#
# 官網 sr.hoshivel.com 為純靜態部署；服務路由由 hoshi-svc 的公開資料平面提供，
# 前端以跨源 fetch 呼叫。這是公開來源位址，會進入 client bundle，不得放密鑰。
#
# hoshi-svc 的來源網域（不含路徑、不含尾端斜線）。
# 未設定時採 src/lib/play.ts 的預設 https://svc.hoshivel.com。
PUBLIC_HOSHI_SVC_BASE=https://svc.hoshivel.com
