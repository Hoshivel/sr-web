# sr-web 前端建置期環境變數（複製為 .env 後修改；`PUBLIC_` 前綴才會進 client bundle）
#
# 官網 sr.hoshivel.com 為純靜態部署；服務路由由 hoshi-svc 的公開資料平面提供，
# 前端以跨源 fetch 呼叫。這是公開來源位址，會進入 client bundle，不得放密鑰。
#
# hoshi-svc 的來源網域（不含路徑、不含尾端斜線）。
# 未設定時採 src/lib/play.ts 的預設 https://svc.hoshivel.com。
#
# **本機開發要把它指到本機的 hoshi-svc**：
#
#   PUBLIC_HOSHI_SVC_BASE=http://localhost:26710
#
# 指到正式網域的話，本機的 Play 會拿到正式的遊戲位址——開發機上那一份 SR
# 沒有任何路徑碰得到。`play.ts` 只在**這個變數自己是 loopback** 時才放行
# `http:` 的節點 URL，所以正式建置不受影響（判準寫在該檔的
# ALLOW_LOOPBACK_NODES）。
#
# 本機那一份 hoshi-svc 用它自己的 `config.example.json` 就對得上：那份的
# sr-game 節點指向 localhost:26603（SR 前端）與 localhost:26600（SR 後端），
# allowedOrigins 放行本站的 26610。
PUBLIC_HOSHI_SVC_BASE=https://svc.hoshivel.com

# 預覽影片的外部存儲來源（不含尾端斜線）。
#
# **影片不隨網站部署**：官網產物只有靜態 HTML／JS／CSS，影片放物件儲存或媒體 CDN，
# 這裡只填去哪裡拿。要播哪幾段寫在 src/config/media.ts 的 PREVIEW_CLIPS。
#
#   PUBLIC_MEDIA_BASE=https://<你的桶或 CDN 網域>/sr/previews
#
# 本機開發同樣指向外部存儲即可。**不要**為了本機方便把影片丟進 public/：
# 那個目錄會被原封不動複製進 dist/，等於連影片一起部署上去
# （.gitignore 擋得住 git，擋不住 dist/）。真要離線開發，另起一個靜態檔案伺服器
# 並把本變數指過去，例如 PUBLIC_MEDIA_BASE=http://localhost:26619。
#
# **未設定時首頁不輸出預覽區塊**（寧可不顯示，也不留壞掉的 <video>）。
PUBLIC_MEDIA_BASE=
