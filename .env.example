# sr-web 前端建置期環境變數（複製為 .env 後修改；`PUBLIC_` 前綴才會進 client bundle）
#
# 官網 sr.hoshivel.com 為**純靜態部署**：沒有反向代理可把 /api/ 導到後端，
# 因此分流 API 由獨立網域提供，前端以跨源 fetch 呼叫。
#
# 分流 API 的來源網域（不含路徑、不含尾端斜線）。
# 未設定時採 src/lib/play.ts 的預設 https://api.hoshivel.com。
# 設為空字串＝同源呼叫（自行以反向代理把 /api/ 導到後端的部署才這樣設）。
PUBLIC_SR_API_BASE=https://api.hoshivel.com
