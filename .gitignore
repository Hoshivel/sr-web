# build output
dist/
# generated types & cache
.astro/
# `npx wrangler dev` 的本機狀態。本機不安裝 wrangler（見 wrangler.jsonc 開頭），
# 但要親眼確認未匹配路徑真的拿得到 404 頁時，只有它跑得起 Workers 的資產路由。
.wrangler/
# deps
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# environment
.env
.env.production
*.local

# 影片一律放外部存儲，**不要放進 public/**。
# public/ 會被原封不動複製進 dist/，也就是部署產物——擺在這裡等於連同上百 MB
# 的影片一起上傳，.gitignore 擋得住 git 卻擋不住 dist/。
# 來源設定見 src/config/media.ts 與 .env.example 的 PUBLIC_MEDIA_BASE。
# 本規則只是最後一道保險，防止有人又把影片丟回來。
public/media/
# macOS / editor
.DS_Store
.vscode/
!.vscode/extensions.json
